# Keep `docker build` contexts rooted at the repo small: none of these are
# needed inside a build. This file applies only to Docker build contexts.
# melange/apko `--source-dir` builds are unaffected.
.git
.github
.claude
target
**/target
